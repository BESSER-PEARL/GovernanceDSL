Scopes: 
    Projects :
        testProject from GitHub : owner/repo
Participants:
    Roles : maintainer
LazyConsensusPolicy testPolicy {
    Scope: testProject
    DecisionType as BooleanDecision
    Participant list : maintainer
    Parameters :
        fallback : MajorityPolicy fallbackPolicy {
            DecisionType as BooleanDecision
            Participant list : maintainer
            Conditions:
                Deadline reviewDeadline : 7 days
        }
}